// Svelte 5 uses runes mode automatically. No special options are required here,
// but this file is the conventional place to add compiler options if needed.
export default {
    compilerOptions: {},
};
